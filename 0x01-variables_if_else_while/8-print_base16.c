#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int c;
	char lowerCase;

	for (c = '0'; c <= '9'; c++)
		putchar(d);
	for (lowerCase = 'a'; lowerCase <= 'f'; lowerCase++)
		putchar(lowerCase);
	putchar('\n');

	return (0);
}
